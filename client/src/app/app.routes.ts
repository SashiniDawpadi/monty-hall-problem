import { Routes } from '@angular/router';
import { MontyHallComponent } from './monty-hall/monty-hall/monty-hall.component';


export const routes: Routes = [
    {
        path: '',
        component: MontyHallComponent
    }
];
